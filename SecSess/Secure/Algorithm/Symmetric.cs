namespace Open.Net.SecSess.Secure.Algorithm
{
    /// <summary>
    /// Symmetric key algorithm to use
    /// </summary>
    public enum Symmetric
    {
        None = 0,
        DES = 1,
        TripleDES = 2,
        AES = 3,
    }
}
